/*
You can't add a HTML file to an arduino, but it is possible to add the website here as a const.
use the website from the receiver as start point in Github -> EasterEdgeRobotics -> Electrical_2026 -> (change brach to profiler) -> jonesy ->  receiver/Receiver -> data
a few changes will be needed to make it work

Also, the UI needs to be changed to add the liquid density as an editable field, a button to add RGB LED in the profile, a button to request last profile data
*/